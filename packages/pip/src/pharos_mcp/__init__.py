"""
PHAROS MCP — One-command install for MCP server discovery.

pip install pharos-mcp gives you:
  - The PHAROS Discovery MCP Server (pharos-mcp command)
  - The pharos CLI binary (bundled as package data)
  - Post-install client configuration detection
"""
__version__ = "0.1.0"
